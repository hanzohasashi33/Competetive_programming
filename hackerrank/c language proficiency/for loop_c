#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() 
{
    int a, b;
    scanf("%d\n%d", &a, &b);
  	// Complete the code.
    
    char *num_arr[10] = {"one","two","three","four","five","six","seven","eight","nine"};
    int i = 0;
    for(i = a;i <= b;i++)
    {
        if(i <= 9)
        {
            printf("%s",num_arr[i - 1]);
        }
        else
        {
            if(i % 2 == 0)
            {
                printf("even");
            }
            else 
            {
                printf("odd");
            }
        }
        printf("\n");
    }

    return 0;
}

