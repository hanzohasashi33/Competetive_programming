def swap_case(s):
    a = ""
    for letter in s:
        if(letter.isupper() == True):
            a += letter.lower()
        else:
            a += letter.upper()
    return a

if __name__ == '__main__':
    s = raw_input()
    result = swap_case(s)
    print result
